{
  "name": "continents_countries_capitals",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node server.js"
     
    "start": "node index.js",
    "build": "your-build-command",
    "deploy": "npm run build && render deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^6.3.0"
  }
}
